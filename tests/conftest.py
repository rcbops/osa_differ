"""Conftest for pytest."""
import pytest


# @pytest.fixture(autouse=True)
# def no_requests(monkeypatch):
#     """Disable requests module network capability."""
#     monkeypatch.delattr("requests.sessions.Session.request")
